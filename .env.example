# Environment variables for production deployment

# Database (Vercel Postgres will auto-fill this in production)
DATABASE_URL="file:./dev.db"

# Authentication (REQUIRED - Generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-super-secret-key-change-this-min-32-chars"
NEXTAUTH_URL="http://localhost:3000"

# Cron Job Secret (Generate with: openssl rand -base64 32)
CRON_SECRET="your-cron-secret-key-change-this"

# Production Notes:
# - On Vercel, DATABASE_URL will be auto-set by Vercel Postgres
# - Change NEXTAUTH_URL to your Vercel URL: https://your-app.vercel.app
# - Generate strong random secrets for production!
